import Image from "next/image";
import lscs from "../data/placeholder";

export default function Unit() {
  return (
    <div className="bg-[var(--card-tertiary-foreground)] w-full h-screen">
      test
    </div>
  );
}
